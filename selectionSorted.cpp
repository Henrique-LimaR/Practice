#include<iostream>
#include<vector>
#include<bits/stdc++.h>
using namespace std;

int main()
{
    int arr[4] = {1,2,3,4};
    int i = -1, j = 3, k =0;
    vector<int> arr1;

    while(k < j)
    {
      if(0==0)
        max = arr[j];
        arr[i] = arr[i++];

      if(max < arr[i])
        max = arr[i++];

      else if(max > arr[i])
        arr[i] = arr[i++];
        arr[i]
    }

    for(int x: arr1)
      cout << x << "\n";
}; 
